#include<stdio.h>
int main()
{
  printf("the size of is integer %d bytes \n",sizeof(int));
  printf("the size of is float %d bytes \n ",sizeof(float));
  printf("the size of is character %d bytes \n ",sizeof(char));
  printf("the size of is double %ld bytes \n",sizeof(double));
}
